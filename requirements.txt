# ============================================================
# NatyaVeda Analyzer v2 — Requirements
# Tested: Python 3.10 / 3.11 / 3.12  |  PyTorch 2.2  |  CUDA 12.1
#
# DO NOT run plain: pip install -r requirements.txt
# Use the installer instead: python scripts/install.py
# It handles the correct order and MMPose via prebuilt wheels.
# ============================================================

# ── Step 0: setuptools MUST be pinned before everything else ──
# setuptools 82+ removed pkg_resources → breaks mmcv, mim, tf-hub
setuptools>=69.5.1,<81
wheel>=0.41.0
pip>=23.0

# ── PyTorch core (install separately, before this file) ───────
# GPU (CUDA 12.1):
#   pip install torch==2.2.0 torchvision==0.17.0 --index-url https://download.pytorch.org/whl/cu121
# GPU (CUDA 11.8):
#   pip install torch==2.2.0 torchvision==0.17.0 --index-url https://download.pytorch.org/whl/cu118
# CPU only:
#   pip install torch==2.2.0 torchvision==0.17.0 --index-url https://download.pytorch.org/whl/cpu
torch>=2.2.0
torchvision>=0.17.0

# ── MMPose Ecosystem (primary 133-keypoint wholebody extractor) ─
# Installed via scripts/install.py using miropsota prebuilt wheels
# for Python 3.12 compatibility. Manual:
#   pip install setuptools<81
#   pip install --extra-index-url https://miropsota.github.io/torch_packages_builder \
#       mmcv==2.2.0+pt2.2.0cu121
#   pip install mmengine mmdet==3.3.0 mmpose==1.3.1
# Listed here for documentation; installer handles actual install:
mmengine>=0.10.0

# ── VitPose-Plus (HuggingFace — Python 3.12 native, extra module) ─
# Pure transformers, no build step. Adds 133 COCO-WholeBody keypoints
# as secondary/ensemble to MMPose. 30M+ downloads.
# Models: usyd-community/vitpose-plus-huge (best)
#         usyd-community/vitpose-plus-large (balanced)
#         usyd-community/vitpose-plus-base  (fast/CPU)
transformers>=4.40.0
huggingface-hub>=0.22.0
timm>=1.0.0
accelerate>=0.27.0
einops>=0.7.0

# ── TensorFlow + TF Hub (MoveNet Thunder — CPU fallback pose) ──
# tensorflow-cpu avoids CUDA lib conflicts inside containers
# setuptools<81 MUST already be installed before these
tensorflow-cpu>=2.15.0
tensorflow-hub>=0.16.0

# ── MediaPipe (21-point finger hand landmarks for mudra) ────────
mediapipe>=0.10.0

# ── OpenCV HEADLESS — eliminates libGL.so.1 forever ─────────────
# Do NOT use opencv-python (requires libGL). Headless is identical
# for all video/image processing; only GUI display functions differ.
opencv-python-headless>=4.9.0
Pillow>=10.0.0
imageio>=2.31.0
albumentations>=1.3.0

# ── Video & Data Collection ──────────────────────────────────────
yt-dlp>=2024.1.0
scenedetect>=0.6.3
ffmpeg-python>=0.2.0

# ── Data & ML Utilities ──────────────────────────────────────────
numpy>=1.24.0,<2.0.0     # cap at 1.x for MMPose/MediaPipe compat
scipy>=1.11.0
pandas>=2.0.0
scikit-learn>=1.3.0

# ── Training & Logging ───────────────────────────────────────────
wandb>=0.16.0
tensorboard>=2.15.0
pyyaml>=6.0
omegaconf>=2.3.0
rich>=13.0.0
tqdm>=4.65.0

# ── Visualization ────────────────────────────────────────────────
matplotlib>=3.7.0
seaborn>=0.12.0

# ── Testing ──────────────────────────────────────────────────────
pytest>=7.4.0
pytest-cov>=4.1.0

# ── Dev Tools ────────────────────────────────────────────────────
black>=23.0.0
isort>=5.12.0
flake8>=6.0.0
